import 'package:get/get.dart';
class PageChangingAnimation{
  PageChangingAnimation._();

  static const sendTransition = Transition.rightToLeft;
  static const duration = Duration(milliseconds: 600);
}
